DDT:
	系统参数：RTP负载类型上报有问题（始终上报0）（已修正）
        模拟源：自动映射码率。（合路时I路码率为解调单元I路的两倍？）
	      结构体没有数据源类型，但其关联到数据类型的可用与否。
	新平台执行宏会导致平台崩溃，但数值正确。(甲方已修正)
	新加编译码LDPC SERVER有问题。(甲方已修正)
usb：
	系统参数 测距数据处理标志码、测速数据处理标志码不可控无回显
	模拟源   数据源类型“自定义”设置不上

ss1
	上行调制，补偿方式有问题，需要修改甲方源码。补偿周期（补偿频率）如何实现,10~10000,发10000会导致崩溃（1000/设置值）。
	遥测模拟源单元，虚拟填充开关一直不能编辑。
	调制器：IO功率比（-20～20） 结构体类型时uchar 服务端为short 负数无法表示 （已修正）
	遥测：星锁挑点参数定位 有效值16服务端状态上报有问题 回显有问题。
ss2
	上行调制，补偿方式有问题，需要修改甲方源码。补偿周期如何实现,10~10000,发10000会导致崩溃。
	一种情况：校验按照关联关系，有些异常值会漏验。但是冯工依然让按照关联关系校验。
	SS2遥控参数设置显示错位:原因为数据状态结构体，新添 发出指令总数、小环正确总数两项。
	模拟源参数设置部分值显示错误。（已修正）
	系统参数任务标识。
	新平台SS2多个目标宏执行会破坏系统参数设置。（已修正）

	

数传接收单元：IQ功率比，监控接收、发送的数据类型均为整型，且无倍数换算关系，而服务端要求显示浮点数，如何处理？

数据类型为无符号数，怎样同时表示负数与正数？（已解决）

扩频模式数据帧标志码结构体缺失。

扩频模式帧同步码组长度用编辑框表示，还是下拉菜单？

扩频模式固定数类型为无符号char，如何表示可能出现的3～4位16进制数？

任务代号汉字无法正常显示。


tc:
单遥控与ss2共用MCSSSB2TCStatusBlk，其成员：
    USHORT	uiAllCmdSendTotal;		//发出指令总数
    USHORT	uiAllSmallLoopRight;	//小环正确总数
在单遥控时不注释，在ss2时注释。
检前记录---记录的备注补支持中文
